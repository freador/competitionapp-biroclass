<% content_for :title, "Novo Participante" %>

<%= render "shared/page_header",
    title: "Novo Participante",
    breadcrumb: "Adicionar participante",
    subtitle: "Preencha os dados para adicionar um novo participante à competição" %>

<div class="mt-8">
  <%= render "form", participant: @participant %>
</div>

<div class="mt-6">
  <%= link_to participants_path, class: "inline-flex items-center gap-2 text-sm font-['Montserrat'] font-semibold text-black hover:text-[#FFCC00] transition-colors" do %>
    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
      <use href="#heroicon-arrow-right" />
    </svg>
    Voltar para lista de participantes
  <% end %>
</div>
