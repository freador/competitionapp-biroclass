<% content_for :title, "Editar Perfil" %>

<%= render "shared/page_header",
    title: "Editar Perfil",
    breadcrumb: "Atualizar dados",
    subtitle: @user.name %>

<div class="mt-8">
  <%= render "users/form", user: @user, cancel_path: profile_path %>
</div>

<div class="mt-6 flex flex-wrap gap-4">
  <%= link_to profile_path, class: "inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-slate-900 transition-colors" do %>
    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
      <use href="#heroicon-eye" />
    </svg>
    Ver perfil
  <% end %>
  <%= link_to competitions_path, class: "inline-flex items-center gap-2 text-sm font-semibold text-slate-600 hover:text-slate-900 transition-colors" do %>
    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
      <use href="#heroicon-arrow-right" />
    </svg>
    Voltar para competições
  <% end %>
</div>
