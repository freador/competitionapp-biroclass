<% return if competition.blank? %>

<div class="fixed inset-0 z-50 flex items-center justify-center px-4 py-8" data-controller="judge-prompt">
  <div class="absolute inset-0 bg-black/80 backdrop-blur-sm"></div>
  <article class="relative z-10 w-full max-w-lg rounded-2xl border-4 border-black bg-white p-8 text-black shadow-2xl">
    <button type="button"
            class="absolute right-4 top-4 rounded-full border-2 border-black bg-white p-2 text-black transition hover:bg-black hover:text-[#FFCC00]"
            data-action="judge-prompt#close"
            aria-label="Fechar lembrete">
      <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
        <use href="#heroicon-x" />
      </svg>
    </button>
    <p class="text-xs font-['Montserrat'] font-semibold uppercase tracking-[0.3em] text-[#FFCC00]">Convite de jurado</p>
    <h2 class="mt-2 text-2xl font-['Montserrat'] font-bold text-black"><%= competition.title %></h2>
    <p class="mt-3 text-sm text-gray-700">
      Esta competição já está aberta e aguarda suas notas para os participantes. Clique abaixo para acessar o painel e avaliar todos eles.
    </p>
    <dl class="mt-4 grid gap-4 text-sm text-gray-700 sm:grid-cols-2">
      <div>
        <dt class="font-['Montserrat'] font-semibold text-black">Participantes</dt>
        <dd class="mt-1"><%= pluralize(competition.participants.count, "participante") %></dd>
      </div>
      <div>
        <dt class="font-['Montserrat'] font-semibold text-black">Status</dt>
        <dd class="mt-1">
          <%= render "shared/badge", text: competition.status, variant: "success", size: "sm" %>
        </dd>
      </div>
    </dl>
    <div class="mt-6 flex flex-wrap gap-3">
      <%= link_to "Ir para painel de notas",
          judging_competition_path(competition),
          class: "inline-flex flex-1 items-center justify-center rounded-full bg-[#FFCC00] px-5 py-3 text-sm font-['Montserrat'] font-semibold uppercase tracking-wide text-black shadow-lg shadow-[#FFCC00]/20 transition hover:bg-[#FFD633] hover:scale-105",
          data: { turbo: false } %>
    </div>
  </article>
</div>
